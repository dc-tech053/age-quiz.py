# age-quiz.py
Age-quizz requests user input of age. It then prints out a corresponding message. The program uses the 'if - elif - else' control structure to link logical assumptions about age with the data provided.
